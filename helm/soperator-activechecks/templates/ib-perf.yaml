apiVersion: slurm.nebius.ai/v1alpha1
kind: ActiveCheck
metadata:
  name: "ib-perf"
spec:
  checkType: "slurmJob"
  name: "ib-perf"
  dependsOn: [ "create-user-soperatorchecks", "wait-for-topology", "prepull-container-image" ]
  slurmClusterRefName: {{ .Values.slurmClusterRefName | quote }}
  schedule: "35 1-21/4 * * *" # 6 times a day at 01:35, 05:35, 09:35, 13:35, 17:35 and 21:35 UTC
  suspend: {{ .Values.checks.ibPerf.suspend }}
  runAfterCreation: {{ .Values.checks.ibPerf.runAfterCreation }}
  slurmJobSpec:
    sbatchScript: |
{{ tpl (.Files.Get "scripts/ib-perf.sh") . | indent 6 }}
    eachWorkerJobs: true
    maxNumberOfJobs: 200
    jobContainer:
      image: {{ .Values.images.slurmJob | quote }}
      env:
{{ toYaml .Values.jobContainer.env | indent 8 }}
      volumeMounts:
{{ toYaml .Values.jobContainer.volumeMounts | indent 8 }}
      volumes:
{{ toYaml .Values.jobContainer.volumes | indent 8 }}
    mungeContainer:
      image: {{ .Values.images.munge | quote }}
  # Do not add any reactions right now, only monitor
  reactions:
    setCondition: false
    drainSlurmNode: false
    commentSlurmNode: true
