apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "checkscontroller.fullname" . }}-checkscontroller
  labels:
  {{- include "checkscontroller.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.checkscontroller.serviceAccount.annotations | nindent 4 }}