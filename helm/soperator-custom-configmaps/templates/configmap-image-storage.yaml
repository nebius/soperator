{{- if .Values.configMaps.imageStorage.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMaps.imageStorage.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "soperator-custom-configmaps.labels" . | nindent 4 }}
data:
  {{- range .Values.configMaps.imageStorage.files }}
  {{ . }}: |
{{ tpl ($.Files.Get (printf "config-files/%s" .)) $ | indent 4 }}
  {{- end }}
{{- end }}
