---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nodesetpowerstates.slurm.nebius.ai
spec:
  group: slurm.nebius.ai
  names:
    kind: NodeSetPowerState
    listKind: NodeSetPowerStateList
    plural: nodesetpowerstates
    singular: nodesetpowerstate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Reference to the NodeSet
      jsonPath: .spec.nodeSetRef
      name: NodeSet
      type: string
    - description: Number of active nodes
      jsonPath: .status.activeCount
      name: Active
      type: integer
    - description: Ready status
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NodeSetPowerState is the Schema for the nodesetpowerstates API.
          It manages the power state of nodes in a NodeSet for ephemeral node scaling.
          One NodeSetPowerState exists per NodeSet, providing a clear 1:1 mapping.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeSetPowerStateSpec defines the desired state of NodeSetPowerState
            properties:
              activeNodes:
                description: |-
                  ActiveNodes is the list of node ordinals that should be powered on.
                  The power-manager binary updates this list based on Slurm's ResumeProgram/SuspendProgram calls.
                  For example, [0, 3, 5, 7, 12] means nodes with ordinals 0, 3, 5, 7, and 12 should be active.
                items:
                  format: int32
                  type: integer
                type: array
                x-kubernetes-list-type: set
              nodeSetRef:
                description: |-
                  NodeSetRef is the name of the NodeSet this power state applies to.
                  This creates a 1:1 mapping between NodeSetPowerState and NodeSet.
                minLength: 1
                type: string
            required:
            - nodeSetRef
            type: object
          status:
            description: NodeSetPowerStateStatus defines the observed state of NodeSetPowerState
            properties:
              activeCount:
                description: ActiveCount is the current number of nodes that are active
                  (powered on).
                format: int32
                type: integer
              conditions:
                description: |-
                  Conditions represent the observations of a NodeSetPowerState's current state.
                  Known types are: Ready, ScalingInProgress.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              nodeStates:
                additionalProperties:
                  description: NodePowerStateInfo contains the state information for
                    a single node
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the node transitioned
                        to this phase.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        Message provides additional details about the node's state.
                        This is typically used to provide error messages.
                      type: string
                    phase:
                      description: |-
                        Phase indicates the current phase of the node's pod.
                        Maps to corev1.PodPhase: Pending, Running, Succeeded, Failed, Unknown.
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Unknown
                      type: string
                    podName:
                      description: PodName is the name of the pod associated with
                        this node.
                      type: string
                    slurmState:
                      description: |-
                        SlurmState is the node's state as reported by Slurm.
                        Known values are: IDLE, ALLOCATED, MIXED, DOWN, DRAIN, POWER_UP, POWER_DOWN.
                      type: string
                  type: object
                description: NodeStates contains the detailed state of each active
                  node, keyed by ordinal.
                type: object
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed for this NodeSetPowerState.
                  It corresponds to the NodeSetPowerState's generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
