---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: rollingupdatestates.slurm.nebius.ai
spec:
  group: slurm.nebius.ai
  names:
    kind: RollingUpdateState
    listKind: RollingUpdateStateList
    plural: rollingupdatestates
    singular: rollingupdatestate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RollingUpdateState is the Schema for the rollingupdatestates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RollingUpdateStateSpec defines the desired state of RollingUpdateState.
            properties:
              remainingPods:
                items:
                  type: string
                type: array
              statefulSetRef:
                type: string
            required:
            - remainingPods
            - statefulSetRef
            type: object
          status:
            description: RollingUpdateStateStatus defines the observed state of RollingUpdateState.
            properties:
              remainingPodsCount:
                type: integer
            required:
            - remainingPodsCount
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
