apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: dstaroff
  name: controller-spool
spec:
  storageClassName: local-pv
  resources:
    requests:
      storage: 32Gi
  accessModes:
    - ReadWriteMany
