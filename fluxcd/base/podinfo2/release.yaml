
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo2
  namespace: flux-system
spec:
  targetNamespace: podinfo2
  interval: 10m
  chart:
    spec:
      chart: podinfo
      version: '6.9.1'
      sourceRef:
        kind: HelmRepository
        name: podinfo
        namespace: flux-system
      interval: 5m
  values:
    replicaCount: 2
  dependsOn:
    - name: podinfo

