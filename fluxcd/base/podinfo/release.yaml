
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  interval: 10m
  chartRef:
    kind: HelmChart
    name: podinfo
    namespace: podinfo
  values:
    replicaCount: 10
